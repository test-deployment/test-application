sudo: required
language: node_js
node_js:
  - "6"

before_script:
  - npm install -g gulp
script: gulp


addons:
  artifacts:
    debug: true
    paths:
      - ./
    target_paths: $TRAVIS_BUILD_NUMBER
    working_dir: WORKING_DIR
    bucket: test-aws-bucket-appsmart
    s3_region: us-east-1
